# Frontend env vars consumidas en runtime (prefix EXPO_PUBLIC_ obligatorio
# para que Metro las inyecte en el bundle).
# Copiar a .env y completar con valores reales. NO commitear .env.

EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-publishable-key
EXPO_PUBLIC_BACKEND_URL=http://localhost:8000

# Sentry DSN (opcional). Si está vacío, no se reportan errores.
# Crear proyecto en sentry.io → Settings → Client Keys (DSN).
# Setear también en EAS env vars de preview/production para que el AAB
# tenga reporte de crashes activado.
EXPO_PUBLIC_SENTRY_DSN=
